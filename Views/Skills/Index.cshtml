@model IEnumerable<TalentosIT.Web.Models.Skill>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AreaProfissional)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AreaProfissional)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSkill">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSkill">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSkill">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
